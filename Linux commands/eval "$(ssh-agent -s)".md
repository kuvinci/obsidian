 #CI/CD #git #linux #shell #ssh

When you don't have rights to fetch/push
```
eval "$(ssh-agent -s)"
```

```
ssh-add ~/.ssh/id_rsa_eddy
```

```
ssh-add ~/.ssh/id_rsa_github
```

```
ssh-add ~/.ssh/id_access
```